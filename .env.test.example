# Baza testowa Supabase (używana przy uruchamianiu testów E2E)
#
# Lokalnie: skopiuj .env.test.example do .env.test i wklej VITE_SUPABASE_ANON_KEY z .env
#           (lub z "npx supabase status" – sekcja Authentication Keys → Publishable)
#
# Osobny projekt w chmurze: utwórz projekt na supabase.com, skopiuj URL i anon key,
#                           zmigruj schemat: supabase link --project-ref <ref> && supabase db push
#
VITE_SUPABASE_URL=https://<project-id>.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-from-supabase-status-or-env

# Opcjonalnie – do cleanup w teardown (Supabase Dashboard → Settings → API → service_role)
SUPABASE_SERVICE_ROLE_KEY=

E2E_BASE_URL=http://localhost:3001
E2E_TIMEOUT=120000

OPENROUTER_API_KEY=
E2E_USERNAME=
E2E_PASSWORD=
E2E_USERNAME_ID=